import type { Collection, Product, DashboardOverview } from '../types/catalog';

/**
 * File này chỉ giữ chỗ để bạn thêm dữ liệu thật khi cần thử giao diện mà chưa kết nối API.
 * - Cập nhật `mockCollections`, `mockProducts`, `mockDashboard` với dữ liệu từ hệ thống thực tế.
 * - Nếu đã có API hoạt động, bạn có thể xóa hoàn toàn file này hoặc để trống các mảng.
 */

export const mockCollections: Collection[] = [];

export const mockProducts: Product[] = [];

export const mockDashboard: DashboardOverview | null = null;

